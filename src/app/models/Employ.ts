export class Employ{
  file: number;
  name: string;
  lastName: string;
  sex: string;
  salary: number;

  constructor(file: number, name: string, lastName: string, sex: string, salary: number){
    this.file = file;
    this.name = name;
    this.lastName = lastName;
    this.sex = sex;
    this.salary = salary;
  }
}
